import * as React from 'react';
export declare type BigNumberInputProps = {
    decimals: number;
    value: string;
    onChange: (value: string) => void;
    renderInput?: (props: React.HTMLProps<HTMLInputElement>) => React.ReactElement;
    autofocus?: boolean;
    placeholder?: string;
    max?: string;
    min?: string;
};
export declare function BigNumberInput({ decimals, value, onChange, renderInput, autofocus, placeholder, max, min, }: BigNumberInputProps): JSX.Element;
